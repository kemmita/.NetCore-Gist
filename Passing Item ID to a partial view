1. Here in the main view we will consume our partial view


@model IEnumerable<FoodApp.Models.Category>
@{
    ViewData["Title"] = "Index";
}

<div class="form-border text-center">
    <table class="table">
        <thead>
        <tr>
            <th style="border: none;">
                @Html.DisplayNameFor(cat => cat.Name)
            </th>
            <th style="border: none;">
                @Html.DisplayNameFor(cat => cat.DisplayOrder)
            </th>
            <th style="border: none;">
                Info
            </th>
        </tr>
        </thead>
        <tbody>
            @foreach (var cat in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(catName => cat.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(catOrder => cat.DisplayOrder)
                    </td>
                    <td>
                        //utilizing the foreach loop above we itterate and grab the items id
                        //call the partial and then pass the id
                        @Html.Partial("_TableButtonPartial", cat.Id)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

2. Now in the partial view we will inlucde @model int at the top of the file as to accept the id sent 


@model int

<td style="width: 150px;">
    <div class="btn-group" role="group">
        <a type="button" class="btn btn-primary" href="@Url.Action("Edit/" + Model)">
            <i class="fas fa-edit"></i>
        </a>
        // we use @url.Action to specify which action will be retrieved. "+ Model" = the id that was sent with the partial above.
        <a type="button" class="btn btn-success" href="@Url.Action("Details/" + Model)">
            <i class="fas fa-list-alt"></i>
        </a>
        <a type="button" class="btn btn-danger" href="@Url.Action("Delete/" + Model)">
            <i class="fas fa-trash-alt"></i>
        </a>
    </div>
</td>
