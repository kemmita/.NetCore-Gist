1. Go to Startup.cs and add a route 
  app.UseMvc(routes =>
            {
                routes.MapRoute(
                    name: "default",
                    template: "{controller=Home}/{action=Index}/{id?}");
                routes.MapRoute(
                    name: "Book",
                    //here we list the controller and the action
                    template: "{controller=Home}/{action=Book}/{id?}");
            });
   
2. HomeController
   public IActionResult Book()
        {
            return View();
        }

3. View

@{
    ViewData["Title"] = "Book";
}

<h2>Book</h2>

